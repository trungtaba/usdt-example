## USDT transfer example
### Run
+ change file example.env to .env
+ change MODE=MAINNET
+ update infomation (account, privateKey, to, amount) inside test() function in index.js file
+ run ```npm install``` to install dependecies
+ run ```node index.js```